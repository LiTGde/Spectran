# Milestone 2 warning-count clarity revision

Build: `M2-R4.6.1-preview-2026-08-13.3`

Review URL: <http://127.0.0.1:43827/>

Stable predecessor: <http://127.0.0.1:43826/>

Date: 2026-08-13

## Addressed finding

- `M2-UX-R04`: The collapsed warning disclosure and every expanded subgroup
  now use the same additive unit. The visible wording is 17 explanations in
  total, grouped as 7 retained-proportion explanations, 5 action-factor
  explanations, and 5 daylight-efficacy-ratio explanations.
- The UI counts the explanations that are visibly listed. The internal count
  of 22 affected metric rows remains available for diagnostics and future
  audit exports but is no longer mixed into the ordinary warning headings.

No warning content, scientific calculation, metric definition, snapshot, or
export contract changed.

## R verification

```sh
air format --check R/transmission_apply.R R/transmission.R \
  tests/testthat/test-transmission-server.R
git diff --check
Rscript -e 'renv::load(); pkgload::load_all(quiet = TRUE); testthat::test_file(
  "tests/testthat/test-transmission-server.R",
  reporter = "summary",
  load_package = "none")'
Rscript -e 'renv::load(); pkgload::load_all(quiet = TRUE); testthat::test_dir(
  "tests/testthat", reporter = "summary", load_package = "none")'
```

Result: all checks passed under R 4.6.1. The focused test verifies the 17 total
explanations, additive group counts of 7, 5, and 5, exact rendered headings,
and the retained internal count of 22 affected metric rows. The complete CSV,
spectral calculation, normalization, and Shiny server suite also passed.

The full package build, official CIE comparison, and `R CMD check` recorded for
the `.2` predecessor remain applicable because `.3` changes only these visible
count labels and their focused assertions. No dependency was installed or
changed.

## Browser verification

Journey: Neutral 50% with Fraction, Total transmittance, and passive-filter
confirmation, applied to the Zero spectrum.

- The technical details were collapsed by default.
- The collapsed label read `Technical denominator details (17 explanations)`.
- Expanded headings read `Retained proportions (7 explanations)`, `Action
  factors (5 explanations)`, and `Daylight efficacy ratios (5 explanations)`.
- The expanded lists contained exactly 17 bullets.
- Expanded details remained inside the page at 320, 390, 768, 1024, 1200, and
  1440 px. No page-level horizontal overflow occurred.
- Browser diagnostics contained no warning or error entries.

## Review scope

- Development-only: source and filter selectors, Development status, and raw
  revision counters.
- Recommended fixture: Neutral 50% with Zero spectrum. The opaque and
  machine-precision summary variants remain covered by focused R tests.
- Milestone 3 remains paused. Promotion, restore, history, and exports are not
  part of this revision.
- The production wizard and continuously visible normalization preview remain
  scheduled for Milestone 4.


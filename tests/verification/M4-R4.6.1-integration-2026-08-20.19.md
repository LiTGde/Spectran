# Milestone 4 Transmission Export focused revision

Build: `M4-R4.6.1-integration-2026-08-20.19`

English review URL: <http://127.0.0.1:43862/>

German review URL: <http://127.0.0.1:43863/>

Stable predecessor: English <http://127.0.0.1:43860/>, German
<http://127.0.0.1:43861/>

Date: 2026-08-20

## Scope

This focused revision corrects the two Export regressions identified during
the visual-conformity review of build `.18`. Scientific calculations and the
accepted R25 through R27 presentation surfaces are unchanged.

## Changes

- The yellow selected-bundle action now fills the available bundle-card width.
  Its hidden Shiny download target remains keyboard accessible through the
  established native-button bridge.
- The live bundle count now reflects every selection state, including zero.
  Clearing the final item shows `0 files selected` or
  `0 Dateien ausgewählt` and disables the bundle action.

## Verification

- `test-transmission-history-server.R` passes, including new 12-item and
  zero-item count assertions, zero-item action gating, and full-width wrapper
  coverage.
- The full package test suite passes after the focused corrections.
- `devtools::check(document = FALSE, manual = FALSE, cran = FALSE)` completes
  with 0 errors, 0 warnings, and 0 notes.
- `git diff --check` passes.
- English browser check: 13 to 12 to 0 selected files; the zero state disables
  the action; desktop and 320 px button bounds remain inside the bundle card;
  no page-level horizontal overflow at 320 px.
- German browser check: `13 Dateien ausgewählt` to
  `0 Dateien ausgewählt`; the zero state disables the action; the desktop
  primary action fills the available card width.
- Browser diagnostics contain 0 errors and 0 warnings naming a production ID
  beginning with `transmission-` in either language. The acknowledged legacy
  Analysis shared-ID warning remains.

The complete `.18` automated and browser verification remains applicable to
all unchanged plot, table, archive, and export surfaces.

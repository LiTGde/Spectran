# Milestone 1 verification

- Build: `M1-R4.6.1-preview-2026-08-12.2`
- Date: 2026-08-12
- Review URL: `http://127.0.0.1:43823/`
- Stable predecessor retained at: `http://127.0.0.1:43821/`
- Full-app smoke URL: `http://127.0.0.1:43822/`

## Runtime

- R 4.6.1 (2026-06-24)
- renv 1.2.4
- shiny 1.14.0
- readr 2.2.0
- ggplot2 4.0.3
- testthat 3.3.2
- tibble 3.3.1
- scales 1.4.0
- pkgload 1.5.3

`renv::status()` reported: `No issues found -- the project is in a consistent state.`

`devtools`, `rcmdcheck`, and `roxygen2` were not installed in the locked
environment. No exported API or generated documentation changed in this
milestone. Base-R package build and check commands were therefore used.

## R verification commands

```sh
Rscript --no-save --no-restore -e 'pkgload::load_all(".", quiet = TRUE); testthat::test_dir("tests/testthat", reporter = "summary")'
R CMD build --no-build-vignettes --no-manual /absolute/path/to/Spectran
env _R_CHECK_FORCE_SUGGESTS_=false R CMD check --no-manual --no-build-vignettes --ignore-vignettes /temporary/path/Spectran_1.0.6.tar.gz
Rscript --no-save --no-restore -e 'renv::status()'
```

Results:

- Full testthat suite: passed.
- Source build: passed.
- `R CMD check`: `Status: OK`.
- Suggested packages `config` and `rhub` were unavailable and reported as
  informational because force-suggests was disabled.
- Code-file non-ASCII check: passed. En dashes and degree symbols displayed
  by the app are represented with Unicode escapes; em dashes are not used.

## Browser and app verification

- Tested widths: 320, 390, 768, 1024, 1200, and 1440 px.
- No whole-page horizontal overflow at any tested width.
- No audit-table overlap at any tested width.
- Audit tables stack at all widths; unavoidable overflow is local to the
  table region at narrow widths.
- The readiness panel precedes the long form, and invalid states render a
  compact actionable preview note without plot/table placeholders.
- Construction plot uses a 0-1 y-axis, shape plus colour, a vertical legend,
  and accessible alternative text.
- Browser console: no warnings or errors in the final responsive pass.
- Local HTTP smoke checks returned 200 for the stable review build, revised
  review build, and full Spectran app.

Verified workflows included neutral complete coverage, independent lower and
upper tails, large-gap acknowledgement, duplicate/out-of-range failures,
metadata qualification, scattering geometry, parser-change invalidation,
new-source consent reset, the 360/380/780/800 external-bracketing case, and
percent scaling that visibly preserves 0.7 percent as fraction 0.007.

## Deferred test data

Milestone 1 continues to use deterministic synthetic fixtures. The planned
licensed facade/window catalogue and the pinned Spitschan clinical/blue-
blocking catalogue remain Milestone 4 work; no unverified real-world export is
presented as test evidence for this build.

# Milestone 4 catalogue-provenance revision

Build: `M4-R4.6.1-integration-2026-08-14.4`

English review URL: <http://127.0.0.1:43842/>

German review URL: <http://127.0.0.1:43843/>

Stable predecessor:

- English `.3`: <http://127.0.0.1:43840/>
- German `.3`: <http://127.0.0.1:43841/>

Date: 2026-08-14

## Addressed finding

`M4-UX-R10` is addressed by presenting one provenance label according to the
record type:

- Package-backed façade records retain `Source version: 0.6.1` or its German
  equivalent.
- Git-backed Spitschan records show only
  `Pinned Git revision: 3f68efef2101a4eacc4a756e4c44df46dd90a7e5`.
- The German Git label is `Fixierter Git-Stand`.
- The redundant `Source version: Pinned Git commit` value and separating
  semicolon are absent.

The stored `source_version`, `source_commit`, provenance, citations, licences,
and audit values are unchanged. No scientific, normalization, Apply, history,
or export code changed.

## Intended focused review

- In English, inspect IPLUS, LowBlueLights, and FL-41 before Apply.
- Confirm IPLUS still shows source version 0.6.1 and 4 mm thickness.
- Confirm both Spitschan records show the single pinned Git revision label and
  the exact full hash.
- Repeat the Spitschan label check in German.
- Check card wrapping at 320 and 390 px, then sample desktop presentation.
- Sample catalogue selection, readiness, and Apply to confirm no adjacent
  behavior changed.

## Recommended test data

- IPLUS from the façade/window collection.
- LowBlueLights and FL-41 from the Spitschan collection.
- D65 as the incident source.

## Development-only elements

- The visible review build identifier.
- Separate fixed-language review instances.
- Deterministic fixtures and raw revision diagnostics.

## Intentional limitations

- History remains session-local.
- Solar transmittance, g-value, angle-dependent optics, fluorescence modelling,
  and new colour metrics remain outside scope.
- Measurement angle remains audit metadata only.

## Automated R verification

```sh
RENV_CONFIG_SANDBOX_ENABLED=FALSE Rscript -e \
  'devtools::test(filter = "transmission-catalogues", reporter = "summary")'

RENV_CONFIG_SANDBOX_ENABLED=FALSE Rscript -e \
  'devtools::test(reporter = "summary")'

RENV_CONFIG_SANDBOX_ENABLED=FALSE Rscript -e 'devtools::document()'

RENV_CONFIG_SANDBOX_ENABLED=FALSE Rscript -e \
  'Sys.setenv("_R_CHECK_FORCE_SUGGESTS_" = "false", \
    "_R_CHECK_CRAN_INCOMING_" = "false", \
    "_R_CHECK_CRAN_INCOMING_REMOTE_" = "false", NOT_CRAN = "true"); \
  check_dir <- tempfile("spectran-check-m4-4-", tmpdir = "/private/tmp"); \
  dir.create(check_dir); \
  rcmdcheck::rcmdcheck(path = ".", args = "--no-manual", \
    error_on = "never", check_dir = check_dir)'

RENV_CONFIG_SANDBOX_ENABLED=FALSE Rscript -e \
  'renv::snapshot(prompt = FALSE); renv::status()'
```

Results:

- R 4.6.1 on `aarch64-apple-darwin23`.
- Test suite: 673 passed, 0 failed, 0 warned, 0 skipped.
- Focused catalogue and bilingual-label tests passed.
- `R CMD check --no-manual`: 0 errors, 0 warnings, 0 notes.
- `git diff --check`: clean.
- Both `.4` servers returned HTTP 200 before the `renv` snapshot.
- `renv::snapshot()`: lockfile already current.
- `renv::status()`: project consistent.

## Regression coverage

- One façade record and both requested Spitschan records are rendered through
  `transmissionServer()`.
- English tests require the single Git label and reject both `Source version`
  and the old `Pinned Git commit` display for Spitschan records.
- German tests require `Fixierter Git-Stand` and reject `Quellversion` for both
  Spitschan records.
- Every tested Git card must retain the exact pinned commit hash.

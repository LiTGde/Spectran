# Milestone 4 promotion and qualification revision

Build: `M4-R4.6.1-integration-2026-08-15.17`

English review URL: <http://127.0.0.1:43858/>

German review URL: <http://127.0.0.1:43859/>

Date: 2026-08-15

The build is frozen for reviewer use. Both fixed-language URLs returned HTTP
200 after the final R 4.6.1 package check.

## Addressed reviewer findings

- M4-UX-R23: the primary promotion button now wraps inside its responsive
  column. The complete English and German labels, button boundary, and focus
  outline remain inside the column at 320 and 390 px.
- M4-UX-R24: changing Transmittance type now invalidates any earlier
  whole-system qualification. Newly selecting Internal or Unknown always
  requires a fresh acknowledgement, including after an intervening Total
  selection.

## Visible browser verification

- English at 320 px: the promotion button occupied x = 29 to 261 px inside a
  column extending to x = 276 px. Its complete label wrapped to a 62 px-high
  button. The document width was 305 px for a 320 px viewport, with no
  page-level horizontal overflow.
- English at 390 px: the promotion button remained inside its column, returned
  to a 46 px height, and did not introduce page-level overflow.
- German at 320 px: `Transmittiertes Spektrum übernehmen` occupied x = 29 to
  261 px inside the same 262 px-wide column. Its complete label wrapped to a
  62 px-high button without clipping.
- German at 390 px: the complete label remained inside the column and wrapped
  without page-level overflow.
- English FL-41: selecting Unknown and acknowledging the qualification made the
  draft ready. Changing Unknown to Total and then back to Unknown restored the
  qualification control unchecked and changed the guided action to
  `Complete required details`.
- English FL-41: after acknowledging Unknown, changing directly to Internal
  again restored the qualification control unchecked and required Details.
- German FL-41: changing Unknown to Gesamttransmission and back to Unbekannt
  restored the qualification control unchecked and displayed
  `Erforderliche Details vervollständigen`.
- Browser diagnostics contained zero errors and zero warnings naming an ID
  beginning with `transmission-` in both languages. Only the acknowledged
  legacy Analysis shared-ID warnings remain.

## Automated R verification

```sh
Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   options(sass.cache = FALSE); \
   devtools::test( \
     filter = "transmission-server|transmission-history-server", \
     reporter = "summary", stop_on_failure = TRUE)'

Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   options(sass.cache = FALSE); \
   devtools::test(filter = "transmission", reporter = "summary", \
     stop_on_failure = TRUE)'

Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   options(sass.cache = FALSE); \
   devtools::test(reporter = "summary", stop_on_failure = TRUE)'

Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   devtools::document()'

Rscript --vanilla -e \
  'source("renv/activate.R"); options(sass.cache = FALSE); \
   devtools::check(document = FALSE, manual = FALSE, cran = FALSE, \
     error_on = "never")'

RENV_CONFIG_NAMESPACES_CHECK=FALSE Rscript --vanilla -e \
  'source("renv/activate.R"); \
   options(repos = c(CRAN = "https://cloud.r-project.org")); \
   renv::settings$bioconductor.version(NULL); renv::status()'

git diff --check
```

Results:

- R 4.6.1 on `aarch64-apple-darwin23`.
- Consequential versions: shiny 1.14.0, bslib 0.12.0, gt 1.3.0,
  ggplot2 4.0.3, patchwork 1.3.2, photobiologyFilters 0.6.1, zip 3.0.2,
  testthat 3.3.2, devtools 2.5.2, and renv 1.2.4.
- Focused server and history tests passed.
- Full Transmission tests passed.
- Full package tests passed.
- Documentation regeneration passed.
- Package check completed with 0 errors, 0 warnings, and 0 notes.
- `renv::status()` reported a consistent R 4.6.1 project with no issues.
- `git diff --check` is clean.

## Recommended reviewer journeys

- English and German at 320 and 390 px: apply IPLUS, open Promotion and
  History, and confirm that the complete primary promotion label, button
  boundary, and focus outline remain inside the left column.
- English FL-41: select Unknown, acknowledge it, switch to Total, then switch
  back to Unknown. Confirm that the qualification is unchecked, Details is
  required, and Apply uses the established grouped recovery route.
- Repeat Unknown to Internal after acknowledgement and confirm that Internal
  also requires a fresh qualification.
- Repeat the type transitions in German and verify the localized readiness and
  guided-action wording.

## Likely regression surface

- Promotion and History at 320 and 390 px in both languages, including the
  promotion name, restore controls, status messages, focus outline, and
  page-level overflow.
- Total, Internal, and Unknown transitions in ready, stale, promoted, and
  sequential states.
- Details readiness, guided navigation, grouped recovery modal routing, Apply
  gating, and fresh qualification after every newly selected qualified type.
- Literal full Tab and Shift+Tab traversal remains a manual reviewer check.

# Milestone 4 narrow-navigation and recovery revision

Build: `M4-R4.6.1-integration-2026-08-15.16`

English review URL: <http://127.0.0.1:43856/>

German review URL: <http://127.0.0.1:43857/>

Date: 2026-08-15

The build is frozen for reviewer use. Both URLs returned HTTP 200 after the
R 4.6.1 `renv` snapshot.

## Addressed reviewer findings

- M4-UX-R20: programmatic tab selection now observes the actual active tab and
  scrolls it fully inside the narrow tab strip. The calculation uses viewport
  rectangle differences, includes right-side padding, and retries after Shiny
  and Bootstrap updates.
- M4-UX-R22: activating Results Apply while required information is missing now
  opens the grouped recovery modal in promoted and sequential states. Its
  direct Details route works with pointer, Enter, and Space while preserving
  the draft.

## Visible browser verification

- German D65 with IPLUS at 320 px: direct Apply selected Ergebnisse and kept
  the active tab fully visible. The active interval was 153.71 to 253.05 px
  inside a 25 to 265 px navigation viewport.
- German Results forward at 320 px selected `Uebernahme und Verlauf` and kept
  the complete active tab inside the same viewport. Its interval was 75.55 to
  253.00 px.
- After German promotion and sequential Apply, changing Transmittance type to
  Unknown cleared the qualification. Enter on Results Apply opened the grouped
  German recovery modal. Space on its Details route dismissed the modal,
  preserved the draft, selected Details, and kept the active tab fully visible.
- The German Details route was repeated at 390 px with Enter and remained
  fully visible.
- English direct Apply at 320 px selected Results with an active interval of
  175.02 to 253.07 px inside the 25 to 265 px viewport.
- English Results forward selected Promotion and History with an active
  interval of 81.57 to 253.07 px inside the same viewport.
- After English promotion, sequential Apply, and changing the type to Unknown,
  Enter on Results Apply opened the grouped recovery modal. Space selected
  Details, preserved the draft, and kept the active tab fully visible.
- The English Details route was repeated at 390 px with Enter and remained
  fully visible.
- English FL-41 at 320 px used Space on the guided route to select
  Normalization. The complete active tab remained visible.
- Browser diagnostics contained zero errors and zero warnings naming an ID
  beginning with `transmission-` in both languages. Only the acknowledged
  legacy Analysis shared-ID warnings remain.

## Automated R verification

```sh
Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   options(sass.cache = FALSE); \
   devtools::test(filter = "transmission-wizard", reporter = "summary", \
     stop_on_failure = TRUE)'

Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   options(sass.cache = FALSE); \
   devtools::test(filter = "transmission", reporter = "summary", \
     stop_on_failure = TRUE)'

Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   options(sass.cache = FALSE); \
   devtools::test(reporter = "summary", stop_on_failure = TRUE)'

Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   devtools::document()'

Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   devtools::check(document = FALSE, manual = FALSE, cran = FALSE, \
     error_on = "never")'

Rscript --vanilla -e \
  'Sys.setenv(RENV_CONFIG_NAMESPACES_CHECK = "FALSE"); \
   source("renv/activate.R"); \
   options(repos = c(CRAN = "https://cloud.r-project.org")); \
   renv::settings$bioconductor.version(NULL); \
   renv::snapshot(prompt = FALSE); \
   renv::status()'
```

Results:

- R 4.6.1 on `aarch64-apple-darwin23`.
- Consequential versions: shiny 1.14.0, bslib 0.12.0, gt 1.3.0,
  ggplot2 4.0.3, patchwork 1.3.2, photobiologyFilters 0.6.1, zip 3.0.2,
  testthat 3.3.2, devtools 2.5.2, and renv 1.2.4.
- Focused wizard tests passed.
- Full Transmission tests passed.
- Full package tests passed.
- Documentation regeneration passed.
- Package check completed with 0 errors, 0 warnings, and 0 notes.
- `renv::snapshot()` recorded R 4.6.1 and the current compatible project
  library. `renv::status()` reported a consistent project with no issues.
- `git diff --check` is clean.

## Recommended reviewer journeys

- German D65 with IPLUS at 320 and 390 px: direct Apply, Results forward, and
  complete visibility of Ergebnisse and `Uebernahme und Verlauf`.
- English D65 with IPLUS at 320 and 390 px: direct Apply, Results forward, and
  complete visibility of Results and Promotion and History.
- D65 with FL-41 in either language: Apply, promote, Apply sequentially, change
  Transmittance type to Unknown, activate Results Apply, then use the grouped
  modal route to Details with pointer, Enter, and Space.
- Confirm that the modal route preserves the draft, dismisses once, selects
  Details, and leaves the complete active tab visible.

## Likely regression surface

- Programmatic tab changes at 320 and 390 px, especially long right-side labels
  in English and German.
- Direct Apply, Back and forward controls, modal routing, Promotion and History,
  and Export navigation.
- Ready, incomplete, stale, promoted, and sequential states.
- Grouped recovery modal actions for Details and Normalization, including
  pointer, Enter, Space, focus visibility, and exactly-once activation.
- Literal full Tab and Shift+Tab traversal remains a manual reviewer check.

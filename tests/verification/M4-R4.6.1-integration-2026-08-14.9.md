# Milestone 4 guided-flow revision

Build: `M4-R4.6.1-integration-2026-08-14.9`

English review URL: <http://127.0.0.1:43852/>

German review URL: <http://127.0.0.1:43853/>

Date: 2026-08-14

The `.8` review processes ended when the workstation restarted. The `.8`
source remains unchanged, but its former local URLs are no longer running.

## Addressed reviewer findings

- M4-UX-R17: all new guided-flow controls now share one keyboard activation
  path. Enter and Space activate Back, forward, direct Apply, Results forward,
  and modal routing controls exactly once.
- M4-UX-R18: the first-tab destination is now Details. An unresolved action on
  Details says `Complete required details`, or its German equivalent, rather
  than referring to the removed Measurement tab.
- M4-UX-R19: a promoted calculation remains visibly identified as a valid,
  frozen result. A separate invitation explains that Apply creates a new
  sequential calculation with the promoted active source.
- M4-UX-R20: every programmatic tab change scrolls the active tab into view in
  the narrow horizontal strip.
- M4-UX-R21: normalization plots use the responsive endpoint-safe break set
  380, 480, 580, 680, and 780 nm. This keeps both calculation boundaries
  explicit without a 380/400 label collision.

## Visible verification completed before reviewer handoff

- Exact `.9` identity was visible in English and German.
- Importing CIE D65 opened Analysis and unlocked optional Transmission.
- English D65 with IPLUS applied from the first tab using Enter, produced one
  populated result, and selected Results.
- English FL-41 showed `Continue to Details`. Enter opened Details, where the
  unresolved action read `Complete required details`.
- After whole-system qualification, Space advanced from Details to
  Normalization. Independent lower and upper carry choices were applied.
- Space on the Normalization direct-Apply action produced the result and
  selected Results. Enter on the Results forward action selected Promotion
  and History.
- Promotion by Enter created one promoted node. Returning to Results showed a
  visible frozen-result banner and a separate sequential-Apply explanation
  naming the promoted active source.
- In an incomplete FL-41 state, Enter on Results Apply opened the grouped
  missing-information modal. Space on `Open Details` dismissed the modal and
  selected Details.
- German direct Apply worked with Space. At 320 px it selected Ergebnisse and
  scrolled the active tab into the visible part of the tab strip.
- At 320 px the German live normalization plot displayed distinct 380, 480,
  580, 680, and 780 nm labels. The 380/400 collision was absent.
- At 390 px programmatic navigation kept the active Details tab within the
  visible tab-strip bounds.
- German FL-41 displayed `Weiter zu Details` and
  `Erforderliche Details vervollständigen`; Space navigation worked.
- Browser diagnostics contained zero errors and zero warnings naming an ID
  beginning with `transmission-` in both languages. Only the acknowledged
  legacy Analysis shared-ID warnings remain.

## Automated R verification

```sh
Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   options(sass.cache = FALSE); \
   devtools::test(filter = "transmission", reporter = "summary", \
     stop_on_failure = TRUE)'

Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   options(sass.cache = FALSE); \
   devtools::test(reporter = "summary", stop_on_failure = TRUE)'

Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   options(sass.cache = FALSE); \
   devtools::document(); \
   devtools::check(document = FALSE, manual = FALSE, cran = FALSE, \
     error_on = "never")'

Rscript --vanilla -e \
  '.libPaths(c(renv::paths$library(project = getwd()), .libPaths())); \
   renv::snapshot(prompt = FALSE); renv::status()'
```

Results:

- R 4.6.1 on `aarch64-apple-darwin23`.
- Consequential versions: shiny 1.14.0, shinydashboard 0.7.3, bslib 0.12.0,
  gt 1.3.0, ggplot2 4.0.3, patchwork 1.3.2, zip 3.0.2, testthat 3.3.2,
  renv 1.2.4, and plotly 4.12.1.
- Focused Transmission tests passed.
- Full test suite passed.
- Documentation regeneration passed.
- `R CMD check --no-manual`: 0 errors, 0 warnings, 0 notes.
- `git diff --check`: clean.

## Recommended reviewer data and journeys

- D65 with IPLUS for first-tab direct Apply, keyboard activation, Results tab
  visibility, and normal result regression.
- D65 with FL-41 for Details wording, incomplete modal routing, independent
  tail carry, promotion, frozen-result status, and sequential Apply.
- English and German at 320 and 390 px for programmatic tab visibility and
  construction-plot endpoint labels.
- Enter and Space on each guided Back, forward, direct Apply, Results forward,
  and modal route control.

## Likely regression surface

- English and German guided controls in ready, incomplete, stale, promoted,
  and sequential states.
- Exactly-once keyboard activation, visible focus, modal dismissal, and focus
  placement after programmatic navigation.
- Details and Normalization conditional labels for Total, Internal, Unknown,
  scattering, and missing-tail states.
- Frozen promoted result, archived inspection, sequential Apply, restore, and
  stale edits made before promotion.
- Narrow programmatic navigation across all six tabs at 320 and 390 px.
- Endpoint labels across all three repeated live previews, partial and
  complete curves, English and German, and preview PNG output.
- Literal full Tab and Shift+Tab traversal remains a manual reviewer check.

# Milestone 4 archived narrow-title boundary refinement

Build: `M4-R4.6.1-integration-2026-08-20.25`

English review URL: <http://127.0.0.1:43874/>

German review URL: <http://127.0.0.1:43875/>

Superseded predecessor: `.24` at ports 43872 and 43873. Those servers were
stopped after `.25` was confirmed, following the user's request to close old
instances.

Date: 2026-08-20

## Scope

This focused revision addresses the remaining `M4-UX-R31` archived-result
case only. Scientific calculations, result data, plot layers, current-result
layout, and PNG export behavior are unchanged.

## Change

- An archived result plot below 260 CSS px now uses a conservative title wrap
  width of 16 characters and retains the existing 10-character hard-word
  wrap.
- The same narrow archived plot reserves a 16-point right margin so that a
  final glyph does not sit against the clipping boundary.
- The exact English title wraps as `CIE Standard`, `Illuminant D`, and
  `6500K × IPLUS`.
- The exact German title wraps as `Norm-`, `Tageslicht`, `spektrum 6500K`, and
  `× IPLUS`.
- Current, stale, and frozen current-result titles continue to use their
  established responsive behavior. PNG export continues to use the accepted
  export title layout.

## Verification

- Focused presentation tests pass: 89 tests, 0 failures, 0 warnings, and
  0 skips.
- The full package test suite passes: 940 tests, 0 failures, 0 warnings, and
  0 skips.
- `devtools::check(document = FALSE, manual = FALSE, cran = FALSE)` completes
  with 0 errors, 0 warnings, and 0 notes.
- Unit tests reconstruct the exact English and German titles after removing
  the intentional line breaks.
- A desktop browser journey covered German import, current Apply, panel B,
  promotion, and the frozen archive. Browser diagnostics contain 0 errors,
  0 warnings naming a production ID beginning with `transmission-`, and
  0 non-legacy warnings in English and German.
- Exact 320 and 390 CSS px archive rendering remains the focused reviewer
  surface. The local viewport override did not alter the browser window's
  reported width, so this record does not claim a visual narrow-width pass.

## Exact R verification commands

```r
devtools::load_all("."); testthat::test_file("tests/testthat/test-transmission-presentation.R")
devtools::test()
devtools::check(document = FALSE, manual = FALSE, cran = FALSE)
```

## Verification environment

- R 4.6.1
- renv 1.2.4
- shiny 1.14.0
- ggplot2 4.0.3
- patchwork 1.3.2
- gt 1.3.0
- testthat 3.3.2
- devtools 2.5.2

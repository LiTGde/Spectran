# Milestone 2 fluorescence-tooltip wording revision

Build: `M2-R4.6.1-preview-2026-08-13.9`

Review URL: <http://127.0.0.1:43833/>

Stable predecessor: <http://127.0.0.1:43832/>

Date: 2026-08-13

## Addressed finding

- `M2-UX-R07`: The Transmittance type tooltip now explains in plain language
  that a fluorescent material can absorb light and re-emit it at different
  wavelengths.
- The tooltip explicitly states that Spectran's wavelength-by-wavelength
  transmission calculation cannot represent that re-emission.
- The Total, Internal, and Unknown definitions remain unchanged.

No tooltip interaction, layout, readiness rule, normalization, Apply behavior,
scientific calculation, metric definition, applied snapshot, or export
contract changed.

## R verification

```sh
air format R/transmission.R tests/testthat/test-transmission-server.R
air format --check R/transmission.R tests/testthat/test-transmission-server.R
git diff --check
Rscript -e 'renv::load(); pkgload::load_all(quiet = TRUE); testthat::test_file(
  "tests/testthat/test-transmission-server.R",
  reporter = "summary", load_package = "none")'
```

Result: formatting and the focused Transmission server tests passed under R
4.6.1. A new assertion protects the novice fluorescence definition. The full
suite and consistent renv result recorded for `.8` remain applicable because
this revision changes only the tooltip sentence and build identifier.

## Browser verification

- The exact `.9` title and build identifier appeared at port 43833.
- The complete novice definition was visible in the Transmittance type
  tooltip.
- The revised tooltip rectangle remained horizontally contained with no page
  overflow at 320, 390, 768, 1024, 1200, and 1440 px.
- At 320 px, the longer tooltip wrapped above the viewport bottom. Focus opened
  it, Escape dismissed it without moving focus or the page, and pointer
  movement from the trigger into the visible text kept it open.
- Browser diagnostics contained no warnings or errors.

## Reviewer regression surface

- Exact wording and novice comprehension of fluorescence and the model limit.
- Wrapping and containment at 320, 390, 768, 1024, 1200, and 1440 px.
- Hover into content, focus, click/tap, Escape dismissal, and re-arming.
- Nearby Transmittance type control and unchanged Internal and Unknown gates.
- Literal Tab and Shift+Tab traversal remains a manual check because the
  browser controller cannot synthesize it reliably.

## Review scope

- Development-only: source and filter selectors, Development status, and raw
  revision counters.
- Plot and table theming and the production wizard remain scheduled for
  Milestone 4.
- Milestone 3 remains paused until the user approves Milestone 2.
